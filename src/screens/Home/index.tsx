import { StatusBar } from 'expo-status-bar';
import * as Styled from './styles';

export const Home = () => {
  return (
    <Styled.Wrapper>
      <StatusBar style="auto" />
    </Styled.Wrapper>
  );
};
